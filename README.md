# ⚽ Football Predictor App

A simple web app that loads real match data from a Google Sheet, analyzes team performance, and predicts match outcomes with AI.

### 🚀 Run locally
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
